export enum UserRole {
  User = 'user',
  Root = 'root',
}

export enum DocumentStatus {
  Empty = 'empty',
  Pending = 'pending',
  Ready = 'ready',
  Error = 'error',
}

export enum DocumentType {
  Youtube = 'youtube',
  Webpage = 'webpage',
}

export enum DocumentAction {
  Load = 'load',
}
